pandas==3.0.1
numpy==2.4.2
scikit-learn==1.8.0
python-dotenv==2.4.2
supabase==2.28.0

# Optional — used for LightGBM LambdaRank re-ranker (falls back to Logistic Regression if absent)
lightgbm==4.6.0

# Optional — used only by embed_courses.py to generate pgvector course embeddings
sentence-transformers==5.2.3
